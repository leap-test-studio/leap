import PageHeader, { Page, PageBody, PageTitle } from "./common/PageHeader";

export default function TestRunner({ windowDimension }) {
  return (
    <Page>
      <PageHeader>
        <PageTitle>Test Runner</PageTitle>
      </PageHeader>
      <PageBody>hi</PageBody>
    </Page>
  );
}
